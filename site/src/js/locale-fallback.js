// locale-fallback.js is intentionally left as a no-op because the site is English-only now.
// The file is kept to avoid accidental build errors if referenced, but it does nothing.
// If you prefer, remove this file and also remove its reference from `docusaurus.config.js`.
